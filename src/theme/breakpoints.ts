import { Breakpoints, createBreakpoints } from '@mui/system';

export const breakpointsTheme: Breakpoints = createBreakpoints({
  values: {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1536,
  },
});
